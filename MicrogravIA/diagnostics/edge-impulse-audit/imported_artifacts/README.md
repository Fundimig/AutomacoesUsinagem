# Imported artifacts

Esta pasta é a área de entrada para cópias dos artefatos originais recuperados
do projeto Edge Impulse `1091379`.

No momento, nenhum dataset, annotation, relatório ou configuração foi
importado.

Quando os downloads forem disponibilizados:

- preservar os arquivos exatamente como recebidos;
- manter versões ou deployments diferentes em diretórios separados;
- não extrair um arquivo por cima de outro;
- não corrigir labels, annotations ou metadados nesta área;
- não armazenar login, token, cookie ou chave de API;
- registrar origem, data e SHA-256 antes de iniciar a análise.

Não adicionar fixtures artificiais ou reconstruções presumidas do formato do
Edge Impulse.
